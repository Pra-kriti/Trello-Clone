import React, { Component } from 'react';
import { Modal, Button } from 'antd';

class Modal extends Component {
    constructor(props) {
        super(props);
        this.state = {
            visible: false
        }
    }
}